# Task: sync-status

> **Audit 2026-09-15 — écart G4** (`.omp/backlog/ImmichSwiftUI-backlog.md:779`) : le client Flutter a un écran dédié au statut de synchronisation — `mobile/lib/pages/settings/sync_status.page.dart` (26 lignes, `SyncStatusPage` → `SyncStatusAndActions`), atteint par la route `SyncStatusRoute` (`mobile/lib/routing/router.dart:182`, `AutoRoute(page: SyncStatusRoute.page, guards: [_duplicateGuard])`) depuis la tuile `SettingSection.beta` de `mobile/lib/pages/common/settings.page.dart:100` (`context.pushRoute(const SyncStatusRoute())`). Côté iOS, aucun écran n'agrège l'état local : les compteurs du ledger et la dernière confrontation serveur vivent dans une section de `BackupSettingsView` (`Sources/Features/Upload/UploadViewModel.swift:495-544`), la progression du run dans une autre (`:651-919`), et l'index hors-ligne nulle part à côté du backup (`Sources/Features/Offline/OfflineAssetsView.swift` ne montre que lui-même). Rien ne relie « suivis / en attente / en échec / hors-ligne » sur une seule surface.

**Objectif** : après cette fiche, l'utilisateur ouvre « Sync Status » depuis le hub « Me » (section Management) et lit en une seule vue l'état réel de la synchronisation locale : combien d'assets le ledger tient pour sauvegardés, combien sont encore en attente dans le run courant, combien sont prêts à partir (file d'upload), combien ont échoué, combien sont épinglés hors-ligne et pour quel volume, quand le run a eu lieu pour la dernière fois et quand le ledger a été confronté au serveur. Il peut relancer un run, l'interrompre, confronter le ledger au serveur, vider le ledger et purger le cache hors-ligne — sans changer d'écran.

**Hors périmètre** :
- L'écran de détail **par asset** d'un run (état, erreur, retry d'un asset précis) : c'est l'écart G5, bande AC-5040–5049 (`upload-detail`). Ici les échecs ne sont listés que par nom + raison, en lecture, via `BackupEngine.failures`.
- **L'export de la base locale et sa réinitialisation** (`export_database`, `reset_sqlite` de `SyncStatusAndActions`) : ces deux actions n'existent que parce que le Flutter tient une réplique Drift. iOS n'a pas de base locale d'assets ; les équivalents réels sont la purge du ledger et la purge du cache hors-ligne, qui sont dans le périmètre.
- Les **jobs `sync_local` / `sync_remote` / `sync_cloud_ids` / `hash_asset`** : ils répliquent la réplique Drift via `POST /api/sync/stream` et hachent en masse pour alimenter cette réplique. iOS n'a ni réplique ni job de hachage séparé — le hachage est une étape interne de `BackupEngine.run()` (`Sources/Services/BackupEngine.swift:240-300`). Le seul job local est donc le run de sauvegarde lui-même.
- La **réparation** d'un ledger incohérent ou d'un index hors-ligne désynchronisé : l'index se réconcilie déjà seul au chargement (`Sources/Services/OfflineAssetStore.swift:296-320`), le ledger se réconcilie par `reconcileNow()`. Aucun écran de réparation n'est ajouté.
- L'import d'une **couleur de statut** ou d'un nouveau composant DesignSystem : l'écran réutilise `PVStatusBadge` et les tokens existants.

**Hypothèses** (vérifiées dans le dépôt et contre `immich-app/immich@main` = `e55ac299`, le 2026-09-15) :

- **Upstream — l'écran et ses briques** :
  - `mobile/lib/pages/settings/sync_status.page.dart` : `SyncStatusPage` est un `Scaffold` dont le `body` est `const SyncStatusAndActions()` (ligne 23). Aucune logique dans la page.
  - `mobile/lib/widgets/settings/beta_sync_settings/sync_status_and_actions.dart` (378 lignes) : un `ListView` à deux blocs. Bloc 1 = `_SyncStatsCounts`, cinq **paires** d'`EntityCountTile` (`assets` local/remote, `albums` local/remote, `other` memories/hashed) issues de `driftProvider` ; bloc 2 = `SettingGroupTitle(t.jobs)` avec 4 `SettingListTile` exécutables (`t.sync_local`, `t.sync_remote`, `t.sync_cloud_ids` gardé par `serverVersion.supports(.cloudIdMetadata)`, `t.hash_asset`), puis `SettingGroupTitle(t.actions)` avec `clear_file_cache`, `export_database`, `reset_sqlite`.
  - `mobile/lib/widgets/settings/beta_sync_settings/entity_count_tile.dart` (73 lignes) : la tuile de comptage — icône 14 pt + libellé centrés en haut, compteur en bas en 18 pt avec *zero-padding* (`zeroPadding(count, maxDigits)`) et `Spacer()` entre les deux. Deux par ligne, à parts égales (`Expanded` + `crossAxisAlignment: Axis.stretch`).
  - Le statut d'un job est un `_SyncStatusIcon` sur `enum SyncStatus { idle, syncing, success, error }` (`sync_status_and_actions.dart:190-205`) : `idle` → rien, `syncing` → `CircularProgressIndicator` 24×24, `success` → `check_circle_outline` vert, `error` → `error_outline` rouge.
  - **Aucun endpoint** n'est appelé par l'écran : les compteurs viennent de la base locale Drift (`assetService.getAssetCounts()`, `localAlbumService.getCount()`, `remoteAlbumService.getCount()`, `memoryService.getCount()`, `assetService.getLocalHashedCount()`) et les actions de `backgroundSyncProvider` / `storageRepositoryProvider`. Vérifié dans `/tmp/immich-openapi-main.json` : les seules routes « sync » du contrat sont `POST /sync/stream` (`operationId: getSyncStream`) et `GET|POST|DELETE /sync/ack` (`getSyncAck` / `sendSyncAck` / `deleteSyncAck`) — c'est le transport de réplication de la base locale Flutter, **pas** ce que lit cet écran. Aucune de ces routes n'est consommée par cette fiche.
- **iOS — l'état local qui remplace la base Drift** :
  - Ledger : `BackupLedger` (`Sources/Services/BackupLedger.swift`), persisté en JSON sous Application Support (`immich-backup-ledger.json`, `:63`). `func trackedCount() -> Int` (`:146`) = les assets suivis ; `var lastReconciliation: Date?` (`:117`) = la dernière confrontation serveur ; `func removeAll()` (`:152`) = la purge.
  - File et compteurs du run : `BackupEngine` (`Sources/Services/BackupEngine.swift`) publie `enum Phase { idle, checking, uploading, done, cancelled }` (`:55`), `phase` (`:66`), `total` (`:69`), `uploadedCount` (`:70`), `rejectedCount` (`:71`), `failedCount` (`:72`), `deferredCount` (`:75`), `stagedCount` (`:83`), `lastError` (`:84`), `currentFileName` (`:92`), `isCancelling` (`:97`), `failures: [BackupFailure]` (`:99`), `processedCount` (`:105`), `examinedCount` (`:109`), `progressFraction` (`:117`).
  - Le ViewModel qui expose déjà tout cela : `UploadViewModel` (`Sources/Features/Upload/UploadViewModel.swift`) — `uploadHistory: [UploadHistoryEntry]` (`:211`), `running` (`:243`), `runBackup(overrideSettings:manual:)` (`:298`), `cancelBackup()` (`:368`), `resumeUpload()` (`:385`), `awaitCurrentBackup()` (`:390`), `trackedAssetCount` (`:399`), `resetBackupTracking()` (`:403`), `lastReconciliation` (`:408`), `reconcileNow()` (`:413`).
  - Index hors-ligne : `OfflineDownloadViewModel` (`Sources/Features/Offline/OfflineDownloadViewModel.swift`) — `cachedAssets: [CachedAssetInfo]` (`:19`), `cacheUsage: Int64` (`:21`), `errorMessage` (`:24`), `formattedUsage(_:)` (`:64`), `load()` (`:78`), `clearAll()` (`:152`), `clearError()` (`:165`). Le miroir observable `OfflineAssetIndex` (`Sources/Features/Offline/OfflineAssetIndex.swift:20,29,30`) expose `items`, `isEmpty`, `count`.
  - Ces deux ViewModels sont **déjà des instances uniques de processus** : `DependencyContainer` (`Sources/DependencyContainer.swift`) tient `let upload: UploadViewModel` (commentaire du champ : « One VM, one run, one island ») et expose `func makeOfflineDownloadViewModel()` (`:212`) qui partage `offlineStore` + `offlineIndex`. Un second engine créé pour cet écran observerait un run différent — c'est exactement le bug que le commentaire de `DependencyContainer` décrit.
  - Précédent de navigation : `OfflineAssetsView` est poussée depuis `ProfileView` et ne déclare **aucun** `NavigationStack` (son doc-comment le dit ; `ProfileView` en porte un, `Sources/Features/Profile/ProfileView.swift:24`). La section qui l'accueille est `Section { … } header: { Text("Management") }` (`ProfileView.swift:55-115`), ligne d'ancrage `OfflineAssetsView(vm: offline)` (`:108`).
- **Ce qui rend la fiche nécessaire** : `grep -n "SyncStatus" Sources/` ne renvoie rien ; aucune vue de `Sources/Features/` n'agrège ledger + file + index, et `Sources/Features/SyncStatus/` n'existe pas (vérifié : `ls .omp/sync-status` → « No such file or directory », et aucun dossier `Sources/Features/SyncStatus`).

**Approche retenue** : A — un `SyncStatusViewModel` (`@MainActor @Observable`) qui **compose** les deux ViewModels de processus déjà existants (`UploadViewModel` pour le ledger + le moteur, `OfflineDownloadViewModel` pour l'index hors-ligne) et n'ajoute aucun état propre, plus une `SyncStatusView` stateless poussée depuis la section Management de `ProfileView`.
- **B (rejetée)** : ajouter une section « Sync status » à `BackupSettingsView` au lieu d'un écran → rejetée pour deux raisons mesurables. (1) Périmètre : l'écran doit couvrir l'index hors-ligne et sa purge, qui n'appartiennent à aucun écran de backup — `BackupSettingsView` ne connaît pas `OfflineDownloadViewModel`. (2) Lisibilité : `BackupSettingsView` porte déjà la progression vivante du run (`progressSection`, `activeProgress`, `currentItemRow`, `segmentBar`, `completionSummary` — `UploadViewModel.swift:651-919`, ~270 lignes) ; y imbriquer l'état durable (ledger, dernière exécution, index hors-ligne) mélange « ce qui se passe maintenant » et « ce que l'appareil contient », et l'upstream garde précisément ces deux surfaces séparées (`SettingSection.backup` vs `SettingSection.beta`).
- **C (rejetée)** : un `SyncStatusViewModel` autonome qui reçoit directement `any BackupLedgerStoring`, `OfflineAssetStore` et un `BackupEngine` neuf → rejetée parce que le dénominateur d'un run (`total`) n'est connu que de l'engine qui l'exécute : un second engine afficherait « 0 en attente » pendant qu'un run est en cours dans `container.upload`, et deux engines se disputent la Live Activity (le commentaire du champ `upload` de `DependencyContainer` documente ce bug). `BackupLedger` et `OfflineAssetStore` sont, eux, déjà partagés — les relire directement ne sert à rien.

## Étapes

1. **Le ViewModel d'agrégation** — NEW `Sources/Features/SyncStatus/SyncStatusViewModel.swift` : `import Foundation`, `import Observation`. Un type `@MainActor @Observable final class SyncStatusViewModel` avec `private let upload: UploadViewModel`, `private let offline: OfflineDownloadViewModel`, et `init(upload: UploadViewModel, offline: OfflineDownloadViewModel)`. Aucun état stocké : **toutes** les propriétés sont des projections calculées.
   - Compteurs du ledger et du run : `var trackedCount: Int { upload.trackedAssetCount }`, `var pendingCount: Int { max(0, upload.engine.total - upload.engine.processedCount) }`, `var stagedCount: Int { upload.engine.stagedCount }`, `var uploadedCount: Int`, `var alreadyOnServerCount: Int { upload.engine.rejectedCount }`, `var failedCount: Int`, `var deferredCount: Int`, `var waitingForWiFi: Bool { upload.engine.deferralReason.contains(.waitingForWiFi) }`, `var waitingForICloud: Bool { upload.engine.deferralReason.contains(.waitingForICloud) }`, `var failures: [BackupFailure] { upload.engine.failures }`, `var currentFileName: String? { upload.engine.currentFileName }`.
   - Index hors-ligne : `var offlineCount: Int { offline.cachedAssets.count }`, `var offlineBytes: Int64 { offline.cacheUsage }`, `func formattedOfflineBytes() -> String { offline.formattedUsage(offline.cacheUsage) }`.
   - Exécutions : `var phase: BackupEngine.Phase { upload.engine.phase }`, `var isRunning: Bool { upload.running }`, `var isCancelling: Bool { upload.engine.isCancelling }`, `var progressFraction: Double { upload.engine.progressFraction }`, `var lastRun: UploadHistoryEntry? { upload.uploadHistory.last }`, `var lastServerCheck: Date? { upload.lastReconciliation }`.
   - Erreurs et état vide : `var runErrorMessage: String? { upload.engine.lastError }`, `var offlineErrorMessage: String? { offline.errorMessage }`, `var hasPendingWork: Bool { isRunning || pendingCount > 0 || stagedCount > 0 }`, `var isEmpty: Bool { trackedCount == 0 && offlineCount == 0 && lastRun == nil && !hasPendingWork }`.
   - Actions, chacune déléguant au ViewModel qui possède l'état : `func runNow() async` (appelle `upload.resumeUpload()` puis `await upload.awaitCurrentBackup()`), `func cancelRun()` (`upload.cancelBackup()`), `func reconcileNow() async` (`await upload.reconcileNow()`), `func resetLedger()` (`upload.resetBackupTracking()`), `func clearOfflineCache() async` (`await offline.clearAll()`), `func clearOfflineError()` (`offline.clearError()`), `func refresh() async` (`await offline.load()`).
   - Un helper d'affichage non localisé pour la date, sur le modèle de `LongDateFormatter` déjà présent dans le dépôt : `func formattedLastServerCheck() -> String` et `func formattedLastRun() -> String`, qui formatent `lastServerCheck` / `lastRun?.date` avec `Date.formatted(date: .abbreviated, time: .shortened)` et renvoient `String(localized: "Never")` quand la valeur est absente.
2. **L'écran** — NEW `Sources/Features/SyncStatus/SyncStatusView.swift` : `import SwiftUI`. `struct SyncStatusView: View` prenant `@Bindable var vm: SyncStatusViewModel`, déclarant `@State private var showResetLedgerConfirm = false` et `@State private var showClearOfflineConfirm = false`. Corps : `ScrollView { VStack(spacing: PVSpacing.s16) { … } .padding(PVSpacing.s16) }` sur `.background(Color.bgPrimary)`, avec `.navigationTitle("Sync Status")`, `.navigationBarTitleDisplayMode(.inline)`, `.task { await vm.refresh() }`, `.refreshable { await vm.refresh() }`, et les deux `confirmationDialog` destructifs. **Aucun `NavigationStack`** : la vue est poussée depuis `ProfileView`, qui en porte déjà un. Identifiants : `syncStatusTrackedValue`, `syncStatusPendingValue`, `syncStatusStagedValue`, `syncStatusFailedValue`, `syncStatusOfflineValue`, `syncStatusLastRunValue`, `syncStatusLastServerCheckValue`, `syncStatusRunNow`, `syncStatusCancel`, `syncStatusReconcile`, `syncStatusResetLedger`, `syncStatusClearOffline`, `syncStatusEmptyState`. Le compteur et la progression sont rendus par un sous-composant privé `SyncCountTile` (icône + libellé en haut, valeur `Font.pvNumeric` en bas, `PVStatusBadge` en pied quand l'état l'exige) et l'en-tête d'état par un sous-composant privé `SyncPhaseHeader`.
3. **L'injection** — EDIT `Sources/DependencyContainer.swift` : ajouter, à côté de `makeOfflineDownloadViewModel()` (`:212`), `func makeSyncStatusViewModel(upload: UploadViewModel, offline: OfflineDownloadViewModel) -> SyncStatusViewModel` qui retourne `SyncStatusViewModel(upload: upload, offline: offline)`. Le paramétrage explicite est le patron déjà en place pour `makeSharedLinkViewerViewModel(baseURL:externalDomain:)` et pour `ProfileView`, qui reçoit des VMs déjà construits ; le conteneur n'a pas à décider quel engine du processus est observé.
4. **Le câblage racine** — EDIT `Sources/RootView.swift` : déclarer `@State private var syncStatus: SyncStatusViewModel` dans `AuthenticatedRoot` (à côté de `@State private var offline: OfflineDownloadViewModel`, `:104`), l'initialiser dans `init(container:)` par `_syncStatus = State(initialValue: container.makeSyncStatusViewModel(upload: container.upload, offline: container.makeOfflineDownloadViewModel()))` (après `_offline = State(initialValue: container.makeOfflineDownloadViewModel())`, `:131`), puis l'ajouter à l'appel `ProfileView(...)` du `.sheet(isPresented: $showProfile)` (`:234`) sous la forme `syncStatus: syncStatus`.
5. **Le point d'entrée** — EDIT `Sources/Features/Profile/ProfileView.swift` : ajouter `@State var syncStatus: SyncStatusViewModel` aux propriétés stockées (après `@State var offline: OfflineDownloadViewModel`, `:20`), et insérer dans la section Management, **immédiatement après** la ligne Backup (`NavigationLink { BackupSettingsView(vm: upload) }`, `:62-68`) et avant la ligne Notifications, une ligne `NavigationLink { SyncStatusView(vm: syncStatus) } label: { Label("Sync Status", systemImage: "arrow.triangle.2.circlepath") }` portant `.accessibilityIdentifier("syncStatusRow")`. L'ancrage après la ligne Backup est délibéré : le statut de synchronisation lit le ledger que l'écran Backup alimente, et la tuile upstream est un voisin direct du backup dans les réglages.
6. **Les tests du ViewModel** — NEW `Tests/SyncStatusViewModelTests.swift` : `import XCTest` et `@testable import ImmichSwiftUI`. Une suite `final class SyncStatusViewModelTests: XCTestCase` qui fabrique ses dépendances comme `Tests/UploadViewModelTests.swift` le fait déjà (`MockImmichClient`, `MockBackupAssetSource`, `BackupSettingsStore(suiteName: "…-\(UUID().uuidString)")`, `BackupLedger.inMemory()`), plus un `OfflineAssetStore` sur un dossier temporaire et son `OfflineDownloadViewModel`. Huit cas nommés, `@MainActor` : `test_counters_mirrorEngineAndLedger`, `test_pendingCount_excludesSettledAssets`, `test_stagedCount_isTheUploadQueueBacklog`, `test_lastRun_isTheMostRecentHistoryEntry`, `test_offlineCount_andBytes_comeFromTheDownloadViewModel`, `test_resetLedger_emptiesTrackedCount`, `test_clearOfflineCache_emptiesTheIndex`, `test_runNow_settlesTheEngineAndRecordsAHistoryEntry`.
7. **Le catalogue de chaînes** — aucune écriture manuelle dans `Resources/Localizable.xcstrings` : la clé est la chaîne anglaise et l'extraction est faite par Xcode au build. Les chaînes neuves introduites par les étapes 1, 2 et 5 sont : `Sync Status`, `Tracked`, `Waiting`, `Ready to upload`, `Failed`, `Offline`, `Last run`, `Last server check`, `Never`, `Run now`, `Stop`, `Check server`, `Reset tracking`, `Reset tracked assets?`, `The next backup will re-check every photo.`, `Clear offline cache?`, `Purge offline cache`, `Downloaded files will need to be downloaded again.`, `Cancel`, `Nothing to sync yet`, `Back up your library to see its status here.`
8. `xcodegen generate` (deux fichiers source et un fichier de test ont été ajoutés : sans régénération ils ne sont pas compilés), puis la suite complète `-only-testing:ImmichSwiftUITests`.

## Incertitudes à lever à l'implémentation

- **Le libellé exact de la ligne du hub.** `Label("Sync Status", …)` retient le vocabulaire de l'upstream (`t.sync_status`) ; si l'extraction i18n du dépôt a déjà une clé proche, la réutiliser plutôt que d'en créer une. Vérifier par : `python3 -c "import json;d=json.load(open('Resources/Localizable.xcstrings'));print([k for k in d['strings'] if 'ync' in k or 'Status' in k])"`.
- **L'icône de la ligne et de l'en-tête.** `arrow.triangle.2.circlepath` est proposé pour rester distinct de `icloud.and.arrow.up` (Backup, `ProfileView.swift:66`) et de `arrow.down.circle` (Offline Storage, `:109`). L'upstream utilise `Icons.sync_outlined` (une flèche circulaire simple) : si `arrow.triangle.2.circlepath` se lit mal à 17 pt, `arrow.clockwise` est l'équivalent SF Symbol le plus proche. Vérifier par : `python3 -c "print('ok')"` puis contrôle visuel dans la maquette Xcode — aucune commande ne tranche un choix d'icône, c'est un arbitrage de revue.
- **Le comportement de `refresh()` quand le run est en cours.** `UploadViewModel.engine` est déjà observé par SwiftUI via `@Observable` ; `refresh()` ne sert qu'à recharger l'index hors-ligne (`offline.load()`). Si le premier rendu montre `offlineCount` à 0 le temps d'un frame, vérifier que la vue lit bien `offline.cachedAssets` (propriété observée) et non une copie locale. Vérifier par : `grep -n "cachedAssets" Sources/Features/SyncStatus/SyncStatusViewModel.swift`.
- **Le total d'un run terminé.** `engine.total` est remis à sa valeur du run courant et n'est pas conservé après un `reset()` ; si l'écran doit afficher « X sur Y » pour la **dernière** exécution et non pour la courante, la source est `lastRun` (`UploadHistoryEntry.uploaded` / `.total`, `UploadViewModel.swift:180-187`) et non l'engine. Vérifier par : `grep -n "uploaded: engine.uploadedCount\|total: engine.total\|UploadHistoryEntry(" Sources/Features/Upload/UploadViewModel.swift`.
- **La profondeur du lien vers les échecs.** Si `upload-detail` (bande AC-5040–5049) est implémenté avant cette fiche, la ligne « Failed » peut pointer vers son écran ; sinon elle reste une liste locale dépliée. Vérifier par : `ls Sources/Features/UploadDetail/` — absent aujourd'hui, donc le repli (liste locale) est le seul comportement correct à livrer.
- **Le libellé du volume hors-ligne.** `OfflineDownloadViewModel.formattedUsage(_:)` (`OfflineDownloadViewModel.swift:64`) est la seule mise en forme de bytes du dépôt pour cet écran ; `ByteCountFormatter.string(fromByteCount:countStyle: .file)` y est utilisé indirectement. Ne pas en écrire une seconde. Vérifier par : `grep -rn "ByteCountFormatter" Sources/Features/SyncStatus/`.
