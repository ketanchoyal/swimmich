# Task: free-up-space

> **Audit 2026-09-15 — écart G1** (`.omp/backlog/ImmichSwiftUI-backlog.md`) : le client Flutter sait libérer l'espace local des originaux déjà sauvegardés, iOS ne sait pas supprimer un asset de la pellicule.
> Preuve upstream : `mobile/lib/services/cleanup.service.dart` (`CleanupService.getRemovalCandidates`, `deleteLocalAssets`, `getDefaultKeepAlbumIds`), route `CleanupPreviewRoute` (`mobile/lib/routing/router.dart:193`, `AutoRoute(page: CleanupPreviewRoute.page, guards: [_authGuard, _duplicateGuard])`, import `:44`), `mobile/lib/providers/cleanup.provider.dart` (`CleanupState`, `CleanupNotifier`), `mobile/lib/domain/models/config/cleanup_config.dart` (`CleanupConfig`), réglages `mobile/lib/widgets/settings/free_up_space_settings.dart` (`FreeUpSpaceSettings`, `_DatePresetCard`, `_KeepAlbumsSection`, `_DeleteConfirmationDialog`), doc `docs/docs/features/mobile-app.mdx:56-97` § Free Up Space.
> État iOS vérifié : `grep -rn "deleteAssets|PHAssetChangeRequest" Sources/` ne trouve **aucune** suppression locale — les 5 occurrences de `deleteAssets` sont le **serveur** (`ImmichClient.swift:49` → `ImmichAPIClient.swift:154`) et `PHAssetChangeRequest` n'apparaît que pour les **créations** (`Sources/Services/PhotoLibraryServiceImpl.swift:124,146`). `Sources/Features/FreeUpSpace/` n'existe pas.

**Objectif** : après cette fiche, l'utilisateur ouvre « Free Up Space » depuis le hub « Me » (section Management), choisit une date de coupure (ou un raccourci 30/60/90 j, 1/2/3 ans), décide de conserver ou non les favoris, les albums choisis (WhatsApp et compagnie pré-cochés) et une catégorie entière de médias, lance un **scan** qui ne retient que les originaux présents **à la fois** sur l'appareil et sur le serveur, **revue** la liste des assets à supprimer avec le volume récupérable, puis confirme une suppression **par lots** qui passe par la corbeille système.
Les albums iCloud partagés sont écartés d'office, parce qu'iCloud ne permet pas d'en retirer les éléments depuis l'appareil.
La suppression côté serveur n'est jamais appelée : l'asset reste sur Immich.

**Hors périmètre** :
- **Supprimer côté serveur** : `deleteAssets(ids:force:)` (`ImmichClient.swift:49`) existe déjà et est utilisé par Timeline/Albums/Trash/Duplicates/PhotoViewer. Cette fiche ne l'appelle jamais — c'est exactement l'inverse du sujet, dont la promesse est « l'original ne vit plus que sur Immich ».
- **Re-télécharger un asset supprimé depuis le serveur** : c'est la surface « On this device » / hors-ligne (`Sources/Features/Offline/`), écart distinct. Si l'utilisateur vient de libérer la place, il retrouve le fichier via le viewer, pas via cet écran.
- **La migration vers `Optimize iPhone Storage`** : la doc le recommande (contre iCloud comme sauvegarde secondaire, `:86-88`) mais c'est un réglage système, hors app.
- **L'Album Sync** (miroir appareil → albums serveur) : c'est l'écart G2. Free Up Space s'en sert seulement comme préalable documenté (« pour que WhatsApp apparaisse dans la liste, il faut activer Album Sync », doc `:97`), sans le construire.
- **Vider la corbeille système** : impossible depuis l'app ; l'écran le dit en footer (doc `:73-75`).

**Hypothèses** (vérifiées dans le dépôt et contre `immich-app/immich@main` = `e55ac299`, le 2026-09-15) :

- **Upstream — le contrat de service** :
  - `CleanupService.getRemovalCandidates(userId, cutoffDate, {keepMediaType, keepFavorites, keepAlbumIds})` délègue à `LocalAssetRepository.getRemovalCandidates`, qui joint la base Drift locale aux assets distants : **le critère « déjà sauvegardé » est un fait de base locale, pas un appel réseau**.
  - `CleanupService.deleteLocalAssets(localIds)` boucle par lots : `static final int _deleteBatchSize = CurrentPlatform.isAndroid ? 2000 : 10000` — **10 000 sur iOS**. Un lot vide ou dont la suppression ne renvoie rien ne compte pas dans le total.
  - `CleanupService.getDefaultKeepAlbumIds(albums)` retient tout album dont le nom **minusculé contient** l'un de `['whatsapp', 'telegram', 'signal', 'messenger', 'viber', 'wechat', 'line']`.
  - `CleanupConfig` (freezed) : `keepFavorites` = `true`, `keepMediaType` = `AssetKeepType.none`, `keepAlbumIds` = `[]`, `cutoffDaysAgo` = `-1`, `defaultsInitialized` = `false`.
    `cutoffDaysAgo == -1` signifie **pas de date choisie** : `selectedDate` reste nil et `scanAssets()` sort immédiatement sans rien scanner.
  - Persistance : `SettingsRepository` écrit `.cleanupCutoffDaysAgo` / `.cleanupKeepMediaType` / `.cleanupKeepFavorites` / `.cleanupKeepAlbumIds` / `.cleanupDefaultsInitialized`.
  - `CleanupNotifier` (`cleanup.provider.dart`) : `setSelectedDate` recalcule `daysAgo` et **vide `assetsToDelete`** ; `setKeepFavorites` / `setKeepMediaType` / `toggleKeepAlbum` vident aussi la liste — tout changement d'un filtre invalide le scan.
  - `applyDefaultAlbumSelections(albums)` n'applique les albums « messagerie » **qu'une fois** (garde `defaultsInitialized`), et `cleanupStaleAlbumIds(existingAlbumIds)` purge les ids d'albums disparus.
  - `scanAssets` / `deleteAssets` `rethrow` après avoir remis leur drapeau (`isScanning` / `isDeleting`) à `false`. `reset()` ne touche **pas** les filtres persistés.
  - `CleanupPreviewPage(assets:)` reconstruit un `TimelineService` sur la liste et rend un `Timeline(..., groupBy: GroupAssetsBy.day, readOnly: true)`, sans bottom sheet ; le titre est `t.cleanup_preview_title(count: assets.length)`.
  - `FreeUpSpaceSettings` : le sous-titre de la section est **calculé** à partir des filtres actifs (`keepFavorites || keepAlbumIds.isNotEmpty || keepMediaType != none`, `:233-249`) ; la date se choisit par `_DatePresetCard` (30/60/90 jours, 1/2/3 ans, `:436-481`) ; la suppression est gardée par `_DeleteConfirmationDialog(assetCount:cutoffDate:)` (`:679-706`) ; `totalBytes` vient du scan et sert au « réclamable ».
  - Doc `:65` : « Free Up Space will only look for photos and videos **on or before** this date » → borne **inclusive**. Doc `:71` : suppression en lots (10 000 iOS). Doc `:83` : « Assets that are part of an **iCloud Shared Album** are automatically excluded ». Doc `:67` : les albums conservés sont préservés « regardless of other settings ».
- **iOS — le substrat réel** :
  - Aucun appel réseau nouveau n'est nécessaire : `func bulkUploadCheck(_ request: AssetBulkUploadCheckRequest) async throws -> AssetBulkUploadCheckResponse` existe (`Sources/Core/Protocols/ImmichClient.swift:278`), les DTO aussi (`Sources/Core/Types/DTOs.swift:217` `AssetBulkUploadCheckRequest.Item { id, checksum }`, `:225` `AssetBulkUploadCheckResponse.Result { id, action }`), et l'implémentation (`Sources/Services/ImmichAPIClient.swift:673-675` → `POST` sur `ImmichAPI.assets.path("/bulk-upload-check")`).
    `action == "reject"` = le serveur **a déjà** ce checksum.
  - Le « ledger » remplace la base Drift : `any BackupLedgerStoring` (`Sources/Services/BackupEngine.swift:64`) expose `entriesForReconciliation() -> [(id: String, checksum: String)]`, `trackedCount()` (`BackupLedger.swift:146`), `forget(ids:)`, `removeAll()` (`:152`).
  - `BackupEngine` le consomme déjà exactement pour ça : `remaining.filter { !ledger.isBackedUp(id:signature:) }` (`:264`) et le découpage en `checkChunkSize = 100` autour de `bulkUploadCheck` (`:623-633`). Le scan de cette fiche **mirror** ce découpage, il ne l'invente pas.
  - `PhotoLibraryServiceImpl` sait **créer** via `PHPhotoLibrary.shared().performChanges` (`:123`, `:145`) mais pas **supprimer** ; il sait résoudre les albums et soustraire des ensembles d'identifiants (`excludedAssetIDs(_:)` `:330-345`, `fetchCandidates(in:excluding:)` `:250-325`).
  - `BackupAlbum.SmartID` (`Sources/Core/Protocols/BackupAssetSource.swift:38-43`) ne couvre **pas** les albums iCloud partagés : il n'y a que `screenshots`, `self-portraits`, `bursts`, `videos` — la variante `.albumCloudShared` reste à ajouter.
  - `BackupCandidate` (`Sources/Core/Protocols/BackupAssetSource.swift:12-24`) porte `id`, `kind`, `fileName`, `fileCreatedAt`, `fileModifiedAt`, `duration`, `isFavorite`, `isLivePhoto` — **aucune taille en octets**. Le « réclamable » de l'écran de revue n'est donc pas dérivable de l'existant.
  - Réutilisables tels quels : `BackupThumbnailView` (`Sources/Features/Upload/UploadViewModel.swift:976-1026`, charge une vignette par `localIdentifier`), `AlbumPickerView(title:albums:selection:)` (`:921-945`, `@Binding Set<String>`), `StorageStatsViewModel.format(_:)` (déjà appelé `Sources/Features/Profile/ProfileView.swift:217`), et le patron `@Observable @MainActor final class BackupSettingsStore` adossé à `UserDefaults` (`:24-136`).
  - MVVM strict en place : `DependencyContainer.init()` construit `self.photos = PhotoLibraryServiceImpl()` (`Sources/DependencyContainer.swift:61`) et le passe à `UploadViewModel` (`:72`).
  - `RootView` possède les `@State` de VM (`AuthenticatedRoot`) et `ProfileView` déclare le **seul** `NavigationStack` (`ProfileView.swift:25`) — les vues poussées (`OfflineAssetsView`, `:108`) n'en déclarent aucun.
  - Le hub : section Management = `Section { … } header: { Text("Management") }` (`ProfileView.swift:55-115`), dernière ligne `OfflineAssetsView(vm: offline)` avec `.accessibilityIdentifier("offlineStorageRow")` (`:107-112`) ; le patron d'identifiant (`notificationsRow` `:75`, `languageRow` `:50`) est établi.

**Approche retenue** : A — un `FreeUpSpaceViewModel` (`@Observable @MainActor`) qui **rejoue en deux temps** la logique upstream : le ledger (complété par un `bulkUploadCheck`) décide **quels** assets sont candidats côté serveur, une nouvelle source locale `LocalCleanupSource` décide **lesquels** sont supprimables côté appareil, et deux vues stateless (réglages + revue) montrent puis exécutent la suppression par lots.
- **B (rejetée)** : déduire « déjà sauvegardé » du seul ledger, sans confronter le serveur → rejetée : le ledger est un cache d'écritures, jamais la vérité du serveur. Il peut être **périmé** — `UploadViewModel.reconcileNow()` (`UploadViewModel.swift:413`) et `BackupEngine.reconcileLedgerIfDue` (`BackupEngine.swift:610-633`) existent précisément parce qu'un asset supprimé côté serveur y reste marqué « sauvegardé », cas déjà couvert par `Tests/BackupLedgerReconciliationTests.swift:202-230` (entrée `"trashed"`). Supprimer l'original local sur la foi d'une entrée périmée détruirait la **seule** copie. Le contrôle serveur coûte zéro octet de média : les checksums sont déjà stockés.
- **C (rejetée)** : faire le scan et la suppression entièrement dans `Photos`, en lisant `PHAsset` directement depuis le ViewModel → rejetée : `PhotoLibraryService` existe pour que **rien** hors de `Sources/Services/` n'importe `Photos`, et un ViewModel qui parle `PHAsset` n'est plus testable avec `MockPhotoLibraryService` (`Tests/Mocks/MockPhotoLibraryService.swift:5`). C'est aussi ce qui rend la feature vérifiable : le scan devient une fonction testable (entrées : ledger + cutoff + filtres ; sortie : candidats + octets).

## Étapes

1. **Le type de candidat et le protocole de suppression locale** — NEW `Sources/Core/Protocols/LocalCleanupSource.swift` : `import Foundation`, `import Photos`.
   `struct CleanupCandidate: Identifiable, Equatable, Sendable { let id: String; let kind: BackupAssetKind; let fileName: String; let creationDate: Date; let byteSize: Int64; let isFavorite: Bool }` — le pendant de `BackupCandidate` avec ce qui lui manque ici, la **taille**.
   `struct CleanupScanResult: Equatable, Sendable { let candidates: [CleanupCandidate]; let skippedNotOnServer: Int; let skippedInSharedAlbum: Int; let scannedCount: Int; var reclaimableBytes: Int64 }`.
   `protocol LocalCleanupSource: Sendable { func cleanupCandidates(cutoff: Date, keepFavorites: Bool, keepMediaType: CleanupKeepMediaType, keepAlbumIDs: Set<String>, backedUpIDs: Set<String>) -> CleanupScanResult; func deleteLocalAssets(ids: [String]) async throws -> Int }`.
   `enum CleanupKeepMediaType: String, CaseIterable, Sendable { case none, photos, videos }` — l'équivalent iOS d'`AssetKeepType` — avec `var title: String` renvoyant `"All"` / `"Photos"` / `"Videos"`.
   Le fichier porte types + protocole, comme `BackupAssetSource.swift` porte `BackupCandidate` + `BackupAlbum` + le protocole.
2. **L'implémentation Photos** — EDIT `Sources/Services/PhotoLibraryServiceImpl.swift` : ajouter `extension PhotoLibraryServiceImpl: LocalCleanupSource` **à la suite** de l'extension `BackupAssetSource` existante.
   `cleanupCandidates(...)` : (a) résoudre les `backedUpIDs` en `PHAsset` par tranches (`PHAsset.fetchAssets(withLocalIdentifiers:options:)`, `PHFetchOptions` trié sur `creationDate` DESC et portant le même `predicate`-indice de préchargement que `fetchCandidates`, `:250-258`).
   (b) écarter tout asset appartenant à une collection iCloud partagée — seul point **neuf** côté Photos : `PHAssetCollection.fetchAssetCollections(with: .album, subtype: .albumCloudShared, options: nil)` puis `PHAsset.fetchAssets(in:options:)`, agrégé **une fois** en `Set<String>` avant la boucle, même stratégie que `excludedAssetIDs(_:)` (`:330-345`).
   (c) écarter `keepAlbumIDs` en réutilisant `Self.excludedAssetIDs(_:)` — les ids passés sont ceux de `BackupAssetSource.fetchAlbums()`, donc le même espace d'identifiants, `BackupAlbum.SmartID` inclus.
   (d) appliquer `creationDate <= cutoff` (borne **inclusive**), `!isFavorite` si `keepFavorites`, et `keepMediaType` (`none` ne filtre rien, `photos` ne retient que les vidéos, `videos` ne retient que les photos).
   (e) lire la taille et construire les candidats dans un `autoreleasepool` (la boucle de `fetchCandidates`, `:315-325`, fait déjà ça pour les mêmes raisons).
   `deleteLocalAssets(ids:)` : boucler `for index in stride(from: 0, to: ids.count, by: Self.deleteBatchSize)`, re-résoudre les `PHAsset` du lot, puis `try await PHPhotoLibrary.shared().performChanges { PHAssetChangeRequest.deleteAssets(assets as NSArray) }` ; accumuler la taille résolue de chaque lot réussi et renvoyer le total.
   `private static let deleteBatchSize = 10_000`, avec le commentaire de provenance (`cleanup.service.dart` + doc `:71`).
3. **Le magasin de réglages** — NEW `Sources/Features/FreeUpSpace/CleanupSettingsStore.swift` : `import Foundation`, `import Observation`.
   `@Observable @MainActor final class CleanupSettingsStore`, copie structurelle de `BackupSettingsStore` (`UploadViewModel.swift:24-136`) : `@ObservationIgnored let defaults: UserDefaults`, `init(suiteName: String)`, clés `cutoffKey` / `favoritesKey` / `mediaTypeKey` / `albumsKey` / `initializedKey`, propriétés à `didSet { defaults.set(...) }` : `var cutoffDate: Date?` (persistée en jours depuis maintenant, `-1` = nil, comme `cutoffDaysAgo`), `var keepFavorites = true`, `var keepMediaType: CleanupKeepMediaType = .none`, `var keepAlbumIDs: Set<String> = []`, `var defaultsInitialized = false`.
   `func applyDefaultKeepAlbums(_ albums: [BackupAlbum])` : **si `!defaultsInitialized`**, ajouter les albums dont `name.lowercased()` contient l'un de `Self.messagingAppNames = ["whatsapp", "telegram", "signal", "messenger", "viber", "wechat", "line"]`, puis poser le drapeau — garde et liste reprises **littéralement** de `getDefaultKeepAlbumIds` et `applyDefaultAlbumSelections`.
   `func pruneStaleAlbums(existing: Set<String>)` = l'équivalent de `cleanupStaleAlbumIds`.
4. **Le ViewModel** — NEW `Sources/Features/FreeUpSpace/FreeUpSpaceViewModel.swift` : `import Foundation`, `import Observation`.
   `@Observable @MainActor final class FreeUpSpaceViewModel` avec `let client: any ImmichClient`, `let source: any LocalCleanupSource`, `let albumSource: any BackupAssetSource`, `let ledger: any BackupLedgerStoring`, `var settings: CleanupSettingsStore`, `private static let serverCheckChunkSize = 100`.
   État : `var albums: [BackupAlbum] = []`, `var candidates: [CleanupCandidate] = []`, `var isScanning = false`, `var isDeleting = false`, `var scannedCount = 0`, `var skippedNotOnServer = 0`, `var skippedInSharedAlbum = 0`, `var lastDeletedCount: Int?`, `var errorMessage: String?`.
   Calculés : `var canScan: Bool { settings.cutoffDate != nil && !isScanning && !isDeleting }`, `var reclaimableBytes: Int64 { candidates.reduce(0) { $0 + $1.byteSize } }`, `var hasScanResults: Bool { scannedCount > 0 && !candidates.isEmpty }`, `var keepSummary: String` (l'agrégat du sous-titre upstream : albums conservés + favoris + catégorie).
   `func loadAlbums()` : `albums = albumSource.fetchAlbums()`, puis `settings.pruneStaleAlbums(existing: Set(albums.map(\.id)))` et `settings.applyDefaultKeepAlbums(albums)`.
   `func scan() async` : `guard let cutoff = settings.cutoffDate else { return }` ; `isScanning = true` ; `errorMessage = nil` ; `candidates = []` ; puis (1) `let entries = ledger.entriesForReconciliation()` ; (2) confirmation serveur, boucle d'index identique à `BackupEngine.reconcileLedgerIfDue` (`BackupEngine.swift:623-633`) : `while index < entries.count { let chunk = Array(entries[index..<min(index + Self.serverCheckChunkSize, entries.count)]) … ; let response = try await client.bulkUploadCheck(AssetBulkUploadCheckRequest(assets: chunk.map { .init(id: $0.id, checksum: $0.checksum) })) ; for result in response.results where result.action == "reject" { backedUpIDs.insert(result.id) } }` ; (3) `let result = await Task.detached(priority: .utility) { source.cleanupCandidates(cutoff:keepFavorites:keepMediaType:keepAlbumIDs:backedUpIDs:) }.value` — hors du MainActor, comme `BackupEngine.run()` (`:252-254`) : une photothèque entière ne doit pas bloquer le run loop ; (4) affecter `candidates`, `scannedCount`, `skippedNotOnServer`, `skippedInSharedAlbum`, `isScanning = false` ; `catch` → `errorMessage`, `isScanning = false`.
   `func setCutoff(_:)`, `setKeepFavorites(_:)`, `setKeepMediaType(_:)`, `toggleKeepAlbum(_:)` écrivent dans `settings` **puis vident `candidates` et remettent `scannedCount` à 0** — c'est la règle upstream de `CleanupNotifier`, et elle interdit de supprimer sur la foi d'un scan fait avec d'autres filtres.
   `func deleteConfirmed() async -> Int?` : garde `!candidates.isEmpty` ; `isDeleting = true` ; `do { let deleted = try await source.deleteLocalAssets(ids: candidates.map(\.id)) ; lastDeletedCount = deleted ; candidates = [] ; scannedCount = 0 ; isDeleting = false ; return deleted } catch { errorMessage = … ; isDeleting = false ; return nil }`.
   **Le ledger n'est PAS purgé** : ces assets sont toujours sur le serveur, et si l'utilisateur les restaure depuis « Récents supprimés » (iOS garde 30 jours, doc `:81`), une entrée conservée évite un ré-upload complet. Commenter cette décision dans le code.
   `func clearError()` et `func resetScan()` (vide `candidates` + compteurs, **garde** les filtres — équivalent de `CleanupNotifier.reset()`).
5. **L'écran de réglages et de scan** — NEW `Sources/Features/FreeUpSpace/FreeUpSpaceView.swift` : `import SwiftUI`.
   `struct FreeUpSpaceView: View` avec `@Bindable var vm: FreeUpSpaceViewModel`, `@State private var showSuccess = false`. `NavigationStack` **absent** (vue poussée depuis `ProfileView`).
   Corps : `Form { cutoffSection; keepSection; scanSection; resultSection }` sur `.navigationTitle("Free Up Space")`, `.navigationBarTitleDisplayMode(.inline)`, `.task { vm.loadAlbums() }`.
   `cutoffSection` : `DatePicker("Cutoff date", selection: binding, in: ...Date(), displayedComponents: .date)` derrière un `Binding<Date>` non-optionnel (repli `.now` quand `settings.cutoffDate == nil`, l'écriture pose la date réelle), puis un `ScrollView(.horizontal)` de pastilles « 30 days / 60 days / 90 days / 1 year / 2 years / 3 years » appelant `vm.setCutoff(Calendar.current.date(byAdding: .day, value: -N, to: .now))` — réplique de `_DatePresetCard`. Footer : « Only photos and videos on or before this date are considered. »
   `keepSection` : `Toggle("Keep favorites", isOn:)`, `Picker("Keep on device", selection:)` sur `CleanupKeepMediaType.allCases`, et `NavigationLink { AlbumPickerView(title: "Keep albums", albums: vm.albums, selection: binding) }` — réutilisation directe de `AlbumPickerView` (`UploadViewModel.swift:921`), dont la sémantique « sélection = albums à traiter » devient ici « sélection = albums à conserver » : le libellé porte la différence et `keepAlbumIDs` est le `@Binding`. Footer = `vm.keepSummary`, jamais un texte figé.
   `scanSection` : `Button("Scan") { Task { await vm.scan() } }.disabled(!vm.canScan)` en `PVPrimaryButtonStyle` ; `ProgressView` + « Scanning your library… » quand `vm.isScanning` ; `InlineErrorBadge(message:retry:)` quand `vm.errorMessage != nil`.
   `resultSection` (si `vm.scannedCount > 0`) : `LabeledContent("Scanned", …)`, `LabeledContent("To delete", value: "\(vm.candidates.count)")`, `LabeledContent("Reclaimable", value: StorageStatsViewModel.format(vm.reclaimableBytes))`, `LabeledContent("Already gone from the server", …)`, `LabeledContent("Kept in iCloud Shared Albums", …)`, puis `NavigationLink { CleanupReviewView(vm: vm) }` libellé « Review \(vm.candidates.count) items » et `.disabled(vm.candidates.isEmpty)`.
   Footer en deux phrases reprises de la doc : le contenu supprimé part dans la corbeille système (à vider à la main, `:73-75`) et reste disponible dans Immich ; les éléments d'un album iCloud partagé ne sont jamais proposés (`:83`).
   `.alert("Space freed", isPresented: $showSuccess)` affichant `vm.lastDeletedCount`, déclenchée par `.onChange(of: vm.lastDeletedCount)` à la fin d'une suppression réussie depuis l'écran de revue.
6. **L'écran de revue** — NEW `Sources/Features/FreeUpSpace/CleanupReviewView.swift` : `import SwiftUI`.
   `struct CleanupReviewView: View` avec `@Environment(\.dismiss) private var dismiss`, `@Bindable var vm: FreeUpSpaceViewModel`. Pas de `NavigationStack`.
   En-tête : `LabeledContent("Reclaimable", value: StorageStatsViewModel.format(vm.reclaimableBytes))` et le total d'items.
   Corps : `ScrollView { LazyVStack(pinnedViews: .sectionHeaders) { … } }` groupé **par jour** — `Dictionary(grouping: vm.candidates) { Calendar.current.startOfDay(for: $0.creationDate) }` trié décroissant, un `Section` par jour avec en-tête `date.formatted(date: .abbreviated, time: .omitted)` + nombre d'items + volume du jour, et une `LazyVGrid(columns: Array(repeating: GridItem(.flexible(), spacing: PVSpacing.s4), count: 3), spacing: PVSpacing.s4)` où chaque cellule est `BackupThumbnailView(localIdentifier: candidate.id)` (`UploadViewModel.swift:976`) sous `.aspectRatio(1, contentMode: .fill)`.
   Barre d'outils, borne basse : `Button("Free up space", role: .destructive) { vm.showDeleteConfirmation = true }` — **non retenu** : la propriété `showDeleteConfirmation` vit dans la vue, pas dans le ViewModel (l'état de présentation d'un dialogue appartient à la vue, comme `showResetTrackingConfirm` dans `BackupSettingsView`, `UploadViewModel.swift:214`).
   `.confirmationDialog("Delete \(vm.candidates.count) items from this device?", isPresented: $showDeleteConfirm)` avec le volume et la date de coupure en message, bouton `"Delete"` → `Task { if await vm.deleteConfirmed() != nil { dismiss() } }`, `.accessibilityIdentifier("cleanupReviewConfirm")` sur ce bouton.
7. **La fabrique** — EDIT `Sources/DependencyContainer.swift` : (a) à côté de `let photos: PhotoLibraryService` (`:11`), ajouter `let cleanupSource: any LocalCleanupSource` ; (b) dans `init()` (`:56-61`), remplacer `self.photos = PhotoLibraryServiceImpl()` par `let photoLibrary = PhotoLibraryServiceImpl(); self.photos = photoLibrary; self.cleanupSource = photoLibrary` — **une seule** instance, comme `photos` est déjà partagé avec `UploadViewModel` (`:72`), parce que deux objets adossés à `PHPhotoLibrary` rendraient le cache des albums iCloud partagés incohérent d'un écran à l'autre ; (c) ajouter `func makeFreeUpSpaceViewModel() -> FreeUpSpaceViewModel` retournant `FreeUpSpaceViewModel(client: client as any ImmichClient, source: cleanupSource, albumSource: photos as any BackupAssetSource, ledger: upload.engine.ledger, settings: CleanupSettingsStore(suiteName: "freeUpSpace"))`.
   `upload.engine.ledger` est le **même** ledger que le run de sauvegarde — c'est lui qui porte les checksums, un ledger neuf ne saurait rien.
8. **Le câblage racine** — EDIT `Sources/RootView.swift` : déclarer `@State private var freeUpSpace: FreeUpSpaceViewModel` dans `AuthenticatedRoot`, l'initialiser dans `init(container:)` par `_freeUpSpace = State(initialValue: container.makeFreeUpSpaceViewModel())`, au même endroit que les autres `_… = State(initialValue: …)`, puis le passer à l'appel `ProfileView(...)` du `.sheet(isPresented: $showProfile)`.
9. **Le point d'entrée** — EDIT `Sources/Features/Profile/ProfileView.swift` : ajouter `@State var freeUpSpace: FreeUpSpaceViewModel` aux propriétés stockées (après `@State var offline: OfflineDownloadViewModel`, `:20`), et insérer **après** la ligne Offline Storage (`:107-112`, dernière de la section) une ligne `NavigationLink { FreeUpSpaceView(vm: freeUpSpace) } label: { Label("Free Up Space", systemImage: "externaldrive.badge.minus") }` portant `.accessibilityIdentifier("freeUpSpaceRow")`.
   L'ancrage en fin de section est délibéré : Free Up Space agit sur **ce que l'appareil contient**, comme la ligne qui le précède immédiatement, et laisser le cluster Backup/Sync Status intact garde « ce qui monte » et « ce qui descend » séparés.
10. **Les tests du ViewModel** — NEW `Tests/FreeUpSpaceViewModelTests.swift` : `import XCTest` et `@testable import ImmichSwiftUI`.
    Suite `final class FreeUpSpaceViewModelTests: XCTestCase` avec un `MockImmichClient` (`Tests/Mocks/MockImmichClient.swift:6`, `bulkUploadCheckResponse` / `bulkUploadCheckChunks`), un `MockCleanupSource` **local au fichier** (`final class MockCleanupSource: LocalCleanupSource, @unchecked Sendable` : candidats et volume injectables, `deleteError`, `deletedIDs` capturés), un `MockBackupAssetSource` (`Tests/Mocks/MockBackupAssetSource.swift:5`) pour les albums, un `BackupLedger.inMemory()`, et `CleanupSettingsStore(suiteName: "cleanup-\(UUID().uuidString)")`.
    Cas `@MainActor` : `test_scan_keepsOnlyAssetsTheServerStillHas` (le ledger marque `a` et `b`, le serveur ne `reject` que `a` → `candidates == ["a"]`, `skippedNotOnServer == 1`), `test_scan_chunksTheServerCheckAtOneHundred` (250 entrées → `mock.bulkUploadCheckChunks.count == 3`, compteur déjà utilisé par `Tests/BackupEngineTests.swift:370`), `test_scan_requiresACutoffDate` (date nil → aucun appel client, `scannedCount == 0`), `test_changingAFilter_discardsTheScan`, `test_applyDefaultKeepAlbums_runsOnceAndMatchesMessagingNames` (albums « WhatsApp » + « Camera » → `keepAlbumIDs == ["whatsapp-id"]`, second appel sans effet), `test_pruneStaleAlbums_dropsVanishedAlbums`, `test_deleteConfirmed_reportsCountAndClearsCandidates`, `test_deleteFailure_keepsCandidatesAndSurfacesError`, `test_deleteDoesNotTouchTheLedger` (`ledger.trackedCount()` inchangé après suppression).
11. **Le catalogue de chaînes** — EDIT `Resources/Localizable.xcstrings` **par extraction au build, aucune écriture manuelle** : la clé est la chaîne anglaise elle-même, Xcode remplit le catalogue en compilant.
    Chaînes neuves des étapes 3 à 6 : `Free Up Space`, `Cutoff date`, `Only photos and videos on or before this date are considered.`, `30 days`, `60 days`, `90 days`, `1 year`, `2 years`, `3 years`, `Keep favorites`, `Keep on device`, `Keep albums`, `All`, `Photos`, `Videos`, `Scan`, `Scanning your library…`, `Scanned`, `To delete`, `Reclaimable`, `Already gone from the server`, `Kept in iCloud Shared Albums`, `Review %lld items`, `Space freed`, `Delete %lld items from this device?`, `Delete`, `Cancel`.
    Faire passer `Label("Free Up Space", …)` par une `LocalizedStringKey` (comme `ImmichAppBar.title`, `Sources/DesignSystem/Components/ImmichAppBar.swift:23`) — un `String` littéral n'est jamais extrait, la ligne du hub resterait en anglais.
    Les quatre langues du catalogue (fr, de, es, it) se traduisent dans le même fichier une fois les clés extraites ; aucune clé n'est créée à la main.
12. **La régénération du projet et la vérification** — EDIT `ImmichSwiftUI.xcodeproj/project.pbxproj` (fichier **généré** : ne jamais l'éditer à la main) par `xcodegen generate` — quatre fichiers source et un fichier de test sont ajoutés, sans régénération ils ne sont pas compilés. Puis la suite complète `-only-testing:ImmichSwiftUITests`.
    Aucun formateur ni linter à lancer séparément : le dépôt n'en configure aucun.

## Incertitudes à lever à l'implémentation

- **La taille d'un asset sans le charger.** `PHAssetResource` n'expose pas `fileSize` en API publique — le seul accès direct est `value(forKey: "fileSize")` (KVC) ; l'alternative est un export via `PHAssetResourceManager` puis `FileManager.attributesOfItem`. Vérifier par : `xcrun swift -e 'import Photos; print(PHAssetResource.self)'` puis un test sur simulateur avec trois assets de tailles connues.
- **La signature exacte de `PHAssetChangeRequest.deleteAssets`.** Apple l'expose en `deleteAssets(_ assets: NSFastEnumeration)`, donc `[PHAsset]` ponté en `NSArray` en Swift. Vérifier avant d'écrire l'appel par : recherche Xcode (`DocumentationSearch` sur « deleteAssets ») ou `xcrun swift -e 'import Photos; print(PHAssetChangeRequest.self)'`.
- **Le dialogue système de confirmation.** `PHPhotoLibrary.performChanges` avec une suppression déclenche **toujours** l'alerte iOS « Autoriser la suppression de N éléments ? ». Notre `confirmationDialog` s'y ajoute : deux confirmations pour un seul geste. Si la revue le juge lourd, l'étape 6 retire son dialogue et s'en remet à l'alerte système (la revue reste, la confirmation devient système). Vérifier sur appareil — aucune commande ne tranche un arbitrage de revue.
- **Les entrées de ledger sans checksum.** Une entrée migrée du format v1 n'a pas de checksum (`BackupLedger.swift:18-21`) : elle ne peut pas être envoyée à `bulk-upload-check` et serait donc écartée du scan, alors que l'asset est bien sur le serveur. Le sens est sûr (on ne supprime jamais sans preuve) mais l'écran doit le dire en footer. Vérifier par : `grep -n -A 6 "entriesForReconciliation" Sources/Services/BackupLedger.swift` pour voir ce que renvoie une entrée v1.
- **L'`operationId` réel de la route de dédup.** L'appel local est certain (`ImmichAPIClient.swift:673-675`) mais le nom de l'opération dans le contrat publié ne l'est pas. Vérifier par : `jq -r '.paths."/assets/bulk-upload-check".post.operationId' /tmp/immich-openapi-main.json`.
- **Le libellé exact des chaînes de l'étape 11.** Elles sont déduites de la structure de `free_up_space_settings.dart` (`:415-481`) et non lues dans le catalogue de traductions ; si une clé proche existe déjà, la réutiliser plutôt que d'en créer une. Vérifier par : `python3 -c "import json;d=json.load(open('Resources/Localizable.xcstrings'));print([k for k in d['strings'] if 'utoff' in k or 'eclaim' in k or 'eep ' in k])"`.
- **Le coût du scan sur une photothèque entière.** `fetchAssets(withLocalIdentifiers:)` par tranches de 100 sur N entrées de ledger, plus la résolution des albums iCloud partagés, sur 50 000 photos : durée inconnue, et le scan n'a aucune progression. Si le premier essai dépasse quelques secondes, ajouter `var scanProgress: Double` alimenté par la boucle, sur le patron de `BackupEngine.progressFraction` (`:117`). Vérifier par : `grep -n "progressFraction" Sources/Services/BackupEngine.swift` puis chronométrage sur appareil.
- **Le bouton du compteur « Already gone from the server ».** Ces assets sont des entrées de ledger que le serveur n'a plus ; l'action correcte est `UploadViewModel.reconcileNow()` (`:413`), qui les oublie pour qu'ils soient re-uploadés. Si l'écran l'offre, il doit passer par le `UploadViewModel` du conteneur et non par celui-ci — donc recevoir une closure câblée à la fabrique, pas un second `UploadViewModel`. Vérifier par : `grep -rn "reconcileNow" Sources/Features/FreeUpSpace/` — aucun appel aujourd'hui, donc le compteur reste informatif.
- **Les ancrages de `RootView.swift`.** Les étapes 8 citent `AuthenticatedRoot` et le `.sheet(isPresented: $showProfile)` par leur nom, repris de l'ossature documentée en mémoire de projet, sans que j'aie relu le fichier ici. Vérifier les numéros de ligne par : `grep -n "AuthenticatedRoot\|showProfile\|@State private var offline" Sources/RootView.swift`.
