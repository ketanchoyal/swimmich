# Task: v1.5-polish

Status: wip — items 1-3 (editor debounce, albums VM refactor, haptics) shipped; item 4 (i18n via Localizable.xcstrings) is the open D1 finding.

## Plan
Implement the 5 V1.5 polish items persisted in `.opencode/memory.md:116`:
1. Editor `renderPreview()` debounce (injected scheduler, default=0 sync to preserve AC-604/605)
2. Albums: move `addAssets(ids:toAlbumId:)` to `AlbumsViewModel`; picker stops instantiating throwaway `AlbumDetailViewModel` (kills wasted `searchMetadata` call)
3. Haptics gaps: `CreateAlbumSheet` `.success` on create; `PhotoEditorView` aspect-ratio `.selection`, rotate `.impact`, revert `.impact` (tick-trigger pattern matching existing 4 features)
4. i18n: create `Resources/Localizable.xcstrings` (iOS 17 String Catalog), seed EN source strings + FR translations, register in `project.yml`, wrap 2 non-Text literals (`AppLockViewModel localizedReason`, `AssetThumbnailCell accessibilityLabel`)
5. Editor: replace `UIScreen.main.bounds.width` with `.aspectRatio(1, contentMode: .fit)` (deprecated, breaks iPad multi-window)

### Hypotheses (to verify via scout)
- `EditorViewModelTests.swift` AC-604/605 are synchronous and assert `renderCounter` immediately after setter — confirmed (lines 55-66, 70-93).
- `AlbumsViewModel` has `client: any ImmichClient` — confirmed (line 10).
- `AlbumDetailViewModel.addAssets` calls `fetchAssets()` which fires `searchMetadata` — confirmed (lines 77-87, 52-62).
- `AddToAlbumPickerSheet:70` instantiates throwaway `AlbumDetailViewModel` — confirmed.
- `DependencyContainer.makePhotoEditorViewModel` exists around line 47 — TO VERIFY.
- `AssetThumbnailCell:179` has bare accessibility label literals — TO VERIFY.
- `AppLockViewModel:70` `localizedReason: "Unlock PhotoVault"` — confirmed via earlier grep.
- No `.xcstrings` catalog exists — confirmed.
- `project.yml` resources lists only Assets.xcassets + Info.plist — confirmed (lines 22-24).

### Steps
1. Editor VM: add `renderDebounceInterval` + `renderTask`, refactor `renderPreview()` into debounced entry + `renderImmediately()` core. Wire DI default `0.033`.
2. Albums VM: add `addAssets(ids:toAlbumId:)`. Picker: drop throwaway VM.
3. Haptics: tick-trigger `.sensoryFeedback` on CreateAlbumSheet + PhotoEditorView.
4. i18n: create catalog, register, wrap 2 literals.
5. UIScreen fix: one-line `.aspectRatio(1, contentMode: .fit)`.
6. Tests: add `AlbumsTests` cases for `AlbumsViewModel.addAssets` (success + error).
7. Run full suite, then tester + reviewer.

## Acceptance Contract

### Approaches candidates
Per item (see specialist output for full table):
1. Debounce: A(async throttle, breaks tests), C(timestamp guard, breaks tests), **E(injected scheduler, default=0 sync)** ← selected
2. Throwaway VM: **A(move to AlbumsVM)** ← selected, B(skip-fetch flag, pollutes detail VM)
3. Haptics: **A(tick-based sensoryFeedback)** ← selected (matches existing 4-feature pattern)
4. i18n: **A(String Catalog + minimal wrap)** ← selected, B(full explicit wrap, overkill)
5. UIScreen: **A(aspectRatio(1, contentMode: .fit))** ← selected, B(scene proxy, over-engineered)

### Approche retenue + rationale
E/A/A/A/A. See per-item rationale above. E is the only debounce option that preserves AC-604/605 sync test contract without modification.

### Critères

```
### AC-700 [type: regression]
Assertion: Complete test suite passes with all V1.5 changes applied.
Check post-impl: xcodebuild test -scheme ImmichSwiftUI -destination 'platform=iOS Simulator,name=iPhone 15' 2>&1 | grep -E 'Test Suite.*passed|failed|Executed.*tests'
Pre-state attendu: ≥145 tests passed, 0 failures
Post-state attendu: ≥145 tests passed, 0 failures (new tests added → count grows)

### AC-701 [type: regression]
Assertion: AC-604/605 render-on-mutation tests pass unchanged (default renderDebounceInterval=0 → sync path preserved).
Check post-impl: xcodebuild test -scheme ImmichSwiftUI -destination 'platform=iOS Simulator,name=iPhone 15' -only-testing ImmichSwiftUITests/EditorViewModelTests/test_AC_604_renderPreview_triggers_on_mutation -only-testing ImmichSwiftUITests/EditorViewModelTests/test_AC_605_all_mutators_trigger_render 2>&1 | grep -E 'test_AC_60[45].*\*\* (passed|failed)|0 failures|with [12] failures'
Pre-state attendu: both methods pass, 0 failures
Post-state attendu: both methods pass, 0 failures

### AC-702 [type: new]
Assertion: PhotoEditorViewModel exposes configurable renderDebounceInterval defaulting to 0.
Check post-impl: grep -n 'renderDebounceInterval' Sources/Features/Editor/PhotoEditorViewModel.swift
Pre-state attendu: 0 matches
Post-state attendu: ≥1 match, declaration with `= 0` default

### AC-703 [type: new]
Assertion: With renderDebounceInterval > 0, rapid successive mutations produce ≤1 render per interval (cancel-and-reschedule logic present).
Check post-impl: grep -nE 'renderTask\?.*cancel|Task.*isCancelled|renderDebounceInterval' Sources/Features/Editor/PhotoEditorViewModel.swift
Pre-state attendu: 0 cancellation matches
Post-state attendu: ≥1 cancel-and-reschedule construct

### AC-704 [type: new]
Assertion: AlbumsViewModel has addAssets(ids:toAlbumId:) calling client.addAssetsToAlbum directly (no searchMetadata, no fetchAssets). Clears errorMessage at start of call (avoids stale-error false-failure when picker checks errorMessage == nil).
Check post-impl: grep -A 12 'func addAssets' Sources/Features/Albums/AlbumsViewModel.swift
Pre-state attendu: 0 matches (method absent)
Post-state attendu: method present, body clears errorMessage, has client.addAssetsToAlbum, NO searchMetadata/fetchAssets

### AC-705 [type: regression]
Assertion: AddToAlbumPickerSheet.add(to:) no longer instantiates AlbumDetailViewModel.
Check post-impl: grep -n 'AlbumDetailViewModel' Sources/Features/Albums/AddToAlbumPickerSheet.swift
Pre-state attendu: 1 match (line 70 instantiation)
Post-state attendu: 0 matches

### AC-706 [type: new]
Assertion: CreateAlbumSheet emits .success haptic on successful create.
Check post-impl: grep -n 'sensoryFeedback' Sources/Features/Albums/CreateAlbumSheet.swift
Pre-state attendu: 0 matches
Post-state attendu: ≥1 match with .success trigger on create path

### AC-707 [type: new]
Assertion: PhotoEditorView emits `.selection` haptic on aspect ratio selection (distinct type from rotation/revert).
Check post-impl: grep -nE 'sensoryFeedback\(\.selection' Sources/Features/Editor/PhotoEditorView.swift
Pre-state attendu: 0 matches
Post-state attendu: ≥1 match bound to aspect-ratio trigger

### AC-708 [type: new]
Assertion: PhotoEditorView emits `.impact` haptic on 90° rotation buttons (distinct type from aspect-ratio).
Check post-impl: grep -nE 'sensoryFeedback\(\.impact' Sources/Features/Editor/PhotoEditorView.swift
Pre-state attendu: 0 matches
Post-state attendu: ≥1 match bound to rotation trigger

### AC-719 [type: new]
Assertion: PhotoEditorView emits haptic on revert action (distinct from rotation/aspect via dedicated trigger var).
Check post-impl: grep -nE 'revertTick|sensoryFeedback\(\.(impact|success).*revert' Sources/Features/Editor/PhotoEditorView.swift
Pre-state attendu: 0 matches
Post-state attendu: ≥1 match (revertTick declared + bumped on revert tap + sensoryFeedback trigger)

### AC-709 [type: new]
Assertion: Resources/Localizable.xcstrings String Catalog file exists.
Check post-impl: test -f Resources/Localizable.xcstrings && echo EXISTS || echo MISSING
Pre-state attendu: MISSING
Post-state attendu: EXISTS

### AC-710 [type: new]
Assertion: String Catalog declares sourceLanguage="en" and contains ≥60 user-visible string entries.
Check post-impl: python3 -c "import json; d=json.load(open('Resources/Localizable.xcstrings')); print('sourceLanguage=', d.get('sourceLanguage')); print('strings=', len(d.get('strings',{})))"
Pre-state attendu: file missing
Post-state attendu: sourceLanguage="en", strings count ≥60

### AC-711 [type: new]
Assertion: String Catalog contains French translations for ≥40 core feature strings.
Check post-impl: python3 -c "import json; d=json.load(open('Resources/Localizable.xcstrings')); fr=[k for k,v in d.get('strings',{}).items() if 'fr' in v.get('localizations',{})]; print(len(fr))"
Pre-state attendu: file missing
Post-state attendu: ≥40 entries have fr localization

### AC-712 [type: new]
Assertion: Localizable.xcstrings registered in project.yml resources.
Check post-impl: grep -n 'Localizable.xcstrings' project.yml
Pre-state attendu: 0 matches
Post-state attendu: ≥1 match under targets.ImmichSwiftUI.resources

### AC-713 [type: regression]
Assertion: AppLockViewModel localizedReason uses String(localized:) instead of bare literal.
Check post-impl: grep -n 'localizedReason.*String(localized:' Sources/Features/Auth/AppLockViewModel.swift
Pre-state attendu: 0 matches (bare "Unlock PhotoVault")
Post-state attendu: 1 match

### AC-714 [type: regression]
Assertion: AssetThumbnailCell accessibility labels (both "Selected" and "Not selected") use String(localized:).
Check post-impl: grep -c 'String(localized:' Sources/Features/Timeline/AssetThumbnailCell.swift
Pre-state attendu: 0 matches
Post-state attendu: ≥2 matches (both labels wrapped)

### AC-715 [type: new]
Assertion: PhotoEditorView.previewSection does not reference UIScreen.main.
Check post-impl: grep -n 'UIScreen' Sources/Features/Editor/PhotoEditorView.swift
Pre-state attendu: 1 match (line 88)
Post-state attendu: 0 matches

### AC-716 [type: regression]
Assertion: Photo editor preview still renders as a square (aspect ratio constraint preserved).
Check post-impl: grep -n 'aspectRatio(1' Sources/Features/Editor/PhotoEditorView.swift
Pre-state attendu: 0 matches (uses UIScreen.main.bounds.width)
Post-state attendu: ≥1 match with .aspectRatio(1, contentMode: .fit)

### AC-717 [type: new]
Assertion: AlbumsViewModel.addAssets has unit tests covering success + error paths.
Check post-impl: grep -nE 'test_AC_V15.*addAssets|addAssets.*toAlbumId' Tests/AlbumsTests.swift
Pre-state attendu: 0 matches (only AlbumDetailViewModel.addAssets tests exist)
Post-state attendu: ≥2 test methods exercising AlbumsViewModel.addAssets(ids:toAlbumId:)

### AC-718 [type: regression]
Assertion: DependencyContainer wires renderDebounceInterval=0.033 (30fps cap) for production editor VM.
Check post-impl: grep -n 'renderDebounceInterval\s*=\s*0\.033' Sources/DependencyContainer.swift
Pre-state attendu: 0 matches
Post-state attendu: ≥1 match with exactly 0.033
```

### Failure modes (top 3 + quel AC les détecte)
1. **Render debounce breaks sync tests** (HIGH) — if default!=0 or debounce path merges into sync path. Caught by AC-701 + AC-702.
2. **String Catalog breaks build** (HIGH) — malformed JSON, missing sourceLanguage, project.yml typo. Caught by AC-709/710/712 + AC-700 (full suite green).
3. **AddToAlbumPickerSheet silent regression** (MEDIUM) — `albumsVM.errorMessage` checked at wrong moment, dismiss fires on failed add. Caught by AC-704/705/717 (incl. error-path test).

## Vérifications manuelles (hors auto-feedback loop)
- V1 Slider drag fluidity (~30fps, no stutter) — run editor, drag exposure slider rapidly.
- V2 CreateAlbumSheet `.success` haptic fires on create.
- V3 Editor aspect ratio `.selection` haptic fires per toggle.
- V4 Editor rotate `.impact` haptic fires per 90° tap.
- V5 Editor revert `.impact` haptic fires.
- V6 FR locale: tab labels, search placeholder, editor headers, trash alerts, create-album sheet, Face ID prompt all display in French when device language=Français.
- V7 EN fallback: device language=Japanese → all strings English, no raw keys visible.
- V8 iPad multi-window: editor preview remains square, no UIScreen.main deprecation warnings.
- V9 Add-to-Album picker: network tab shows only `POST /api/albums/:id/assets`, NO `POST /api/search/metadata` for the picker action.
